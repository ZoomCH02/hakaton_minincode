// function clearActiveNav() {
//     // Получаем все элементы с классом "active"
//     const activeElements = document.getElementsByClassName("active");

//     // Преобразуем коллекцию в массив, чтобы можно было перебирать элементы
//     Array.from(activeElements).forEach(function (element) {
//         element.classList.remove("active"); // Убираем класс "active"
//     });
// }

// document.addEventListener('DOMContentLoaded', clearActiveNav());
